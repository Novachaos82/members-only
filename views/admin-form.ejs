<!DOCTYPE html>
<html>
  <%- include('./header.ejs') %>
  <body>
    <%- include('./partials/navbar.ejs') %> <% if(!user.admin){ %>
    <div class="form-div">
      <div class="form-box">
        <form action="" method="post" class="form-class">
          <label for="passcode">Passcode</label>
          <input type="text" name="passcode" id="passcode" />
          <button type="submit">submit</button>
        </form>
      </div>
    </div>

    <% }else{ %>
    <h1>You are already an Admin,dawg</h1>
    <% }%>
  </body>
</html>
